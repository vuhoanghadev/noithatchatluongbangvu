// Background image loading completely disabled for maximum performance
document.addEventListener('DOMContentLoaded', function () {
  // This script is intentionally empty to prevent any background image loading
  // The services section now uses a solid color background for maximum performance
  console.log(
    'Services section optimized for performance - background image disabled'
  );
});
